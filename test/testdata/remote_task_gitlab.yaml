apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: remote-task-gitlab
spec:
  steps:
    - name: echo
      image: registry.access.redhat.com/ubi10/ubi-micro
      script: |
        echo "Hello from deeply nested GitLab subgroup"
